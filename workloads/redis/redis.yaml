---
apiVersion: flux.weave.works/v1beta1
kind: HelmRelease
metadata:
  name: redis
  namespace: lucas
  annotations:
    flux.weave.works/automated: "true"
spec:
  releaseName: redis
  chart:
    git: git@github.com:helm/charts
    path: stable/redis
    ref: master
